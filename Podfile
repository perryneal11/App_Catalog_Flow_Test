# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'App Catalog Flow Test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod ‘SmartDeviceLink’
  # Pods for App Catalog Flow Test

  target 'App Catalog Flow TestTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'App Catalog Flow TestUITests' do
    # Pods for testing
  end

end
